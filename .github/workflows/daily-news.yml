# Automatische Artikel-Erstellung dauerhaft deaktiviert
name: Weekly Journal Article (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Nur manuell und nur wenn explizit gewollt'
        required: true
        default: 'nein'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Automation deaktiviert
        run: echo "Automatische Artikel-Erstellung ist dauerhaft deaktiviert."
